# --- Core (required) ---------------------------------------------------
GitPython>=3.1.43
requests>=2.31.0

# --- AI ----------------------------------------------------------------
# Default provider is Google Gemini.
google-generativeai>=0.7.0
# Optional: only needed if you switch ai_provider back to "openai".
# openai>=1.30.0

# --- Watchers ----------------------------------------------------------
watchdog>=4.0.0
psutil>=5.9.0

# --- Tray UI -----------------------------------------------------------
pystray>=0.19.5
Pillow>=10.0.0

# --- Secure secret storage --------------------------------------------
keyring>=24.0.0

# --- Windows integration (installed only on Windows) ------------------
pywin32>=306; sys_platform == "win32"
win10toast>=0.9; sys_platform == "win32"

# --- Optional cross-platform notifications ----------------------------
plyer>=2.1.0
